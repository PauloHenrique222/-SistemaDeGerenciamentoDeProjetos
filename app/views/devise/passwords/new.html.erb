<div class="card form-center center-block">
  <h2>Esqueceu sua senha?</h2>
  <p>Tudo bem, é só preencher o campo abaixo que enviaremos um link para você criar uma nova.</p>
  <br/>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="form-group">
      <%= f.label :email %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control"%>
    </div>

    <div class="actions">
      <%= f.submit "Enviar e-mail para redefinir senha", class:"btn btn-primary btn-block" %>
    </div>
    <br/>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
